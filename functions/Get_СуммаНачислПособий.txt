CREATE FUNCTION Get_СуммаНачислПособий
 (@работникID INT, @Дата DATETIME)
RETURNS MONEY

BEGIN
	DECLARE @Summ MONEY
	
	SET @Summ = (SELECT SUM(ISNULL(Сумма,0)) 
	FROM view_Пособие
	WHERE (@работникID = работникID) AND ([Начало периода] < @Дата) AND ([Конец периода] > @Дата)
	)

	RETURN @Summ
END